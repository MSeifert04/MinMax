using System;
using System.Collections.Generic;
using System.Linq;

using MiSe.MinMax.Extensions;

namespace MiSe.MinMax.Samples
{
    public static class Program
    {
        public static void Main(string[] args)
        {
            // The samples have been moved to linqpad-samples. However this project remains so
            // that things can be easily tested during development.
        }
    }
}
